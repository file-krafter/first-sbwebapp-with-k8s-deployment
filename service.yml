apiVersion: v1
kind: Service
metadata:
  name: ingress-nginx-service
  namespace: my-ingress-namespace
spec:
  selector:
    app: ingress-nginx-controller
  ports:
    - port: 91 # keeping service port as 91
      targetPort: 8080